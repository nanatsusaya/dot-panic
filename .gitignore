node_modules/
.DS_Store
Thumbs.db

# 0009 R1 — the emitted JavaScript is not committed. §3 puts one `.js` beside
# each `.ts`, so the pattern is the three directories 0002 §7 fixes and nothing
# broader: two copies of every module is what 0001 §6.1 punishes, and a reader
# cannot tell which one is authoritative.
core/**/*.js
shell/**/*.js
view/**/*.js
