{
  "name": "dot-panic",
  "private": true,
  "type": "module",
  "scripts": {
    "check:method": "node ../agent-project-rules/checks/check-method.mjs .",
    "check:types": "tsc --noEmit",
    "check:lint": "biome check .",
    "check:test": "bun test",
    "check": "bun run check:method && bun run check:types && bun run check:lint && bun run check:test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.7",
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  }
}
